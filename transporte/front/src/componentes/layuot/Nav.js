import Header from "./Header"

const Nav = (props) => {
    return (
        <nav>
            <h1>
                hola soy la navegacion
            </h1>
        </nav>
    )
}

export default Nav;